---

---

<html lang="en">
	<head>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="/style.css" />


<style is:global>

@import 'https://unpkg.com/open-props' layer(design.system);
@import 'https://unpkg.com/open-props/normalize.dark.min.css'
  layer(base.normalize);

@import url('https://fonts.googleapis.com/css2?family=Mulish:ital,wght@0,200..1000;1,200..1000&family=Tilt+Neon:YROT@-45..45&display=swap');

svg {
	font-family: "Tilt Neon", sans-serif;
	width: 100%; height: 100%;
}
svg text {
	animation: stroke 9s infinite alternate;
	stroke-width: 2;
	stroke: #365FA0;
	font-size: 149px;
}
@keyframes stroke {
	0%   {
		fill: rgba(72,138,204,0); stroke: rgba(54,95,160,1);
		stroke-dashoffset: 25%; stroke-dasharray: 0 50%; stroke-width: 2;
	}
	70%  {fill: rgba(72,138,204,0); stroke: rgba(54,95,160,1); }
	80%  {fill: rgba(72,138,204,0); stroke: rgba(54,95,160,1); stroke-width: 3; }
	100% {
		fill: rgba(72,138,204,1); stroke: rgba(54,95,160,0);
		stroke-dashoffset: -25%; stroke-dasharray: 50% 0; stroke-width: 0;
	}
}

.wrapper {background-color: #FFFFFF};
.tilt-neon-font {
  font-family: "Tilt Neon", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-variation-settings:
    "XROT" 0,
    "YROT" 0;
}


body, html {
  font-family: "Mulish", sans-serif;
}
h1,h2,h3,h4,h5,h6{ font-family: "Tilt Neon", sans-serif;}
  body {
  margin: 0;
  padding: 0;
  font-family: "Mulish", sans-serif;
}

header.hero-section {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: var(--gradient-1);
  text-align: center;
}

/* Tilt Neon Font Styles */
.tilt-neon-font {
  font-family: "Tilt Neon", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
  font-size: clamp(3rem, 8vw, 10rem);
  background: var(--gradient-2);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-variation-settings:
    "XROT" 0,
    "YROT" 0;
}

/* Mulish Font */
.mulish-font {
  font-family: "Mulish", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
}
/* Base Styles */
body {
  margin: 0;
  padding: 0;
  font-family: "Mulish", sans-serif;
background-image: var(--gradient-8);
color:white;
}

header.hero-section {
  display: flex;
  justify-content: center;
      align-items: start;
    margin-top: 0;
    padding-top: 0;
  height: 100vh;
  text-align: center;
}
.gb {
background-image: var(--gradient-5);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Tilt Neon Font Styles */
.tilt-neon-font {
    font-family: Tilt Neon, sans-serif;
    font-optical-sizing: auto;
    font-weight: 400;
    max-width: 100vw;
    white-space: nowrap;
    overflow: hidden;
    word-wrap: break-word;
    text-wrap: wrap;
    letter-spacing: -0.25ch;
    word-break: break-all;
    font-size: 20rem;
    font-style: normal;
    /* font-size: clamp(3rem,8vw,175px); */
    background: var(--gradient-15);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: flex;
    flex-wrap: wrap;
    flex-direction: row;
}

.tilt-neon-letter {
  display: inline-block;
  font-variation-settings:
    "XROT" 0,
    "YROT" 0;
  animation: rotate-y 5s infinite linear;
}

/* Mulish Font */
.mulish-font {
  font-family: "Mulish", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
  font-style: normal;
}

/* Variables for Open Props gradients */

/* Keyframes for Y-axis rotation */
@keyframes rotate-y {
  0% {
    font-variation-settings: "YROT" 0;
  }
  100% {
    font-variation-settings: "YROT" 360;
  }
}

body {
 padding: var(--size-10);
 display: flex;
flex-direction:column;  gap: var(--size-5);
}

</style>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Accelerated</title>
	</head>
	<body>
		<header class="hero-section">
    <h1 class="hero-title hhh tilt-neon-font">Accelerated Impact </h1>
  </header>
  <main>
    <p class="mulish-font">
Hyper-Meta-Modern; Embrace everything.
</p>
<p>
 Bespoke digital solutions that resonate at global scales. Elegant complexity digital fluidity. 
</p>
  </main>
<article>
<style>

</style>
<div class="wrapper">
	<svg>
		<text x="50%" y="50%" dy=".35em" text-anchor="middle">
			Accelerated Impact 
		</text>
	</svg>
</div>
</article>
<footer></footer>
<script>
 function getRandomInRange(min, max) {
    return Math.random() * (max - min) + min;
  }
document.addEventListener('DOMContentLoaded', () => {
 const gradients = [
    'var(--gradient-2)',
    'var(--gradient-3)',
    'var(--gradient-4)',
    'var(--gradient-5)',
 'var(--gradient-6)',
 'var(--gradient-7)',
'var(--gradient-15)',
'var(--gradient-15)',
 'var(--gradient-28)',
 'var(--gradient-9)',
 'var(--gradient-10)',
 'var(--gradient-11)',
 'var(--gradient-12)',
 'var(--gradient-13)',
  ];
  const heroTitle = document.querySelector('.hhh');
  const text = heroTitle.textContent;
  heroTitle.innerHTML = '';

  // Split text into spans
  text.split('').forEach((char, index) => {
    const span = document.createElement('span');
    span.textContent = char;
    span.classList.add('tilt-neon-letter');
    

  span.style.animationDelay = `${index * 0.5}s`;
    span.style.animationDuration = `${10 + index}s`;
        span.style.backgroundImage = gradients[index % gradients.length];
 const heightVariation = getRandomInRange(80, 140);
 const heightVariation2 = getRandomInRange(90, 110);
    span.style.transform = `scaleY(${heightVariation / 100})`;
    span.style.transform = `scaleX(${heightVariation2 / 100})`;
    heroTitle.appendChild(span);
  });
});</script>
	</body>
</html>
